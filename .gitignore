.env
.venv/
__pycache__/
*.pyc
.DS_Store
.idea
.vscode
.pytest_cache/
*.egg-info/
dist/
build/

# Local clones of the MCP server fork — we install it via `uvx --from git+...`,
# never from a local path. Keeps stray .git directories out of the repo index.
mcp-server-qdrant/
mcp-fork/
